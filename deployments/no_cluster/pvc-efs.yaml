apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hostpath-pvc-efs
  namespace: kubeflow
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
